/**
 * This is an example helper module.
 *
 * It contains TypeScript code that does not generate any JS code,
 * i.e. only interface and type definitions.
 *
 * In Sonar, it should have either 100% coverage or no coverage at all.
 */
export interface Foo {
  id: string;
}

export interface Bar {
  id: number;
}
